interface:
  display_name: "Looper Cron Builder"
  short_description: "Build resource-aware loop daemon workflows"
  default_prompt: "Use $looper-cron-builder to build a resource-aware, reward-aware, ROI-tracked loop daemon for this repository."
